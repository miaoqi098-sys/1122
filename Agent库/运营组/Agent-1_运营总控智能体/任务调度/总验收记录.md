# Agent-1 任务调度总验收记录

## 1. 验收范围
- README与模块/共享层边界；
- Task canonical生命周期；
- TaskGraph依赖与循环保护；
- 审批节点；
- 观察窗口与验证；
- failed/cancelled/rollback语义；
- R07/R08/R09边界；
- 静态示例与验收。

## 2. 关键修复
1. 将旧中文生命周期迁移为S07 canonical状态机：`created → pending_approval → ready → running → blocked/failed/cancelled/awaiting_validation → completed/rolled_back`。
2. 用统一scope替代“所有Task必须产品/ASIN”的旧假设。
3. 拆分owner/executor/approver/validator角色。
4. 建立depends_on/blocks/parallel_with/mutex_with及循环依赖阻断。
5. FinalDecision.approval和required_controls可实体化为审批/control/monitoring/rollback任务与条件。
6. 明确执行动作成功后通常进入awaiting_validation，而不是直接completed。
7. 明确blocked、failed、cancelled、rollback trigger、rolled_back的不同语义。
8. 明确Agent-1只定义Task框架，真实调度/执行/结果标准化属于R07/R08/R09。

## 3. L3门禁
- [x] 模块定位与S07职责一致。
- [x] README与正式文件一致。
- [x] Task状态机与S07 V1.2一致。
- [x] FinalDecision→TaskPlan→Task追溯一致。
- [x] TaskGraph关系完整且有循环保护。
- [x] approval/controls不能被绕过。
- [x] observation/validation与S08边界清楚。
- [x] blocked/failed/cancelled/rolled_back无语义混用。
- [x] 共享Scheduler/Executor/Normalizer边界明确。
- [x] 有静态示例与验收。
- [x] 当前无模块级阻塞。

## 4. 运行依赖
无新增独立编号；R07/R08/R09继续作为真实调度、执行和执行结果标准化能力。未来Task状态持久化、超时、重试、并发资源管理也归共享运行层实现。

## 5. 未宣称完成
当前通过L3框架静态验收，不代表Scheduler、Executor、审批系统、Task持久化和S08验证已经真实运行。

## 6. 结论
`任务调度/` 已达到当前Agent库框架阶段L3封板条件。下一模块进入 `历史记忆/` 启动分析。