# Agent-1 决策规则总验收记录

## 1. 验收范围
本记录对 `决策规则/` 一级模块执行 L3 框架验收，范围包括：
- README 与模块职责边界；
- 决策总则；
- 资格与硬约束；
- 动态优先级；
- 策略稳定与反转；
- 最终决策边界；
- 信息不足、审批与降级；
- 示例与静态验收。

## 2. 关键修复
1. 已移除旧的永久固定冲突优先级链，改为“资格门 + 目标层级 + S04 动态排序”。
2. 已把 S06 eligibility 正式纳入最终选择资格门。
3. 已把 S10 StrategyChain 稳定性门禁纳入策略改变场景。
4. 已明确 DecisionSelector 是最终 Option 选择器，S04/S05/S06/S10/S07 不互相越权。
5. 已建立证据不足、来源冲突、审批、低置信和降级路径。
6. 已增加典型冲突示例，验证模块不会机械回退到固定排序表。

## 3. L3 门禁
- [x] 模块定位与 Agent-1 唯一决策出口一致。
- [x] 目录结构已从单一旧规则补为完整治理规则集。
- [x] README 与实际文件一致。
- [x] 与目标系统四层目标和动态排序原则一致。
- [x] 与 S04 Priority Gate / P0-P3 动态业务优先级一致。
- [x] 与 S06 eligibility / required_controls / dominant-risk 规则一致。
- [x] 与 S10 allow/hold/merge/escalate/override 和 StrategyChain 一致。
- [x] 与 DecisionSelector 输入输出边界一致。
- [x] 与 S07 的“决策后任务编排”边界清楚。
- [x] 已覆盖信息不足、审批、冲突证据和降级路径。
- [x] 已有静态示例与验收记录。
- [x] 未把真实 Runner、审批系统、Scheduler、Executor 混入本模块。
- [x] 当前无模块级阻塞。

## 4. 运行依赖
本模块没有新增独立运行依赖编号，继续引用：
- R03 Schema Validator：未来对 DecisionSelector/FinalDecision 等对象做机器校验；
- R07 调度器：未来承接 FinalDecision 后的任务调度；
- R08 执行器：未来执行经营动作；
- R11 StrategyChain 持久化与更新：未来支撑 S10 策略历史和反转判断。

## 5. 未宣称完成的部分
当前通过的是框架层 L3 静态验收，不代表：
- DecisionSelector 已真实运行；
- S10 已接入持久化 StrategyChain；
- 审批流已上线；
- 动态优先级已用真实经营数据回放验证。

## 6. 验收结论
`决策规则/` 已达到当前 Agent库 框架建设阶段的 L3 封板条件。

下一模块应进入 `输入规范/` 启动分析；若后续输入规范发现 canonical Event/ContextPackage 与本模块仍有字段冲突，按真实归属生成修订任务并重新打开对应门禁。