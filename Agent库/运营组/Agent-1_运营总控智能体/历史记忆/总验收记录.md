# Agent-1 历史记忆总验收记录

## 1. 验收范围
- README与S02/S09/案例库/R06边界；
- 产品与经营事实历史；
- FinalDecision历史；
- Task/ExecutionResult历史；
- ValidationResult历史；
- LearningRecord经验历史；
- freshness与复用；
- 历史记忆与Case单一事实源边界；
- 静态示例与验收。

## 2. 关键修复
1. 将旧“产品/ASIN混合历史记录”改为统一scope的分层历史架构。
2. 从事实历史中移除`lesson`式永久结论语义，事实、解释、验证和经验各自归位。
3. 建立Decision版本、supersedes与StrategyChain历史规则。
4. 建立Task状态变化与ExecutionResult历史，区分blocked/failed/执行成功与经营验证成功。
5. 建立ValidationResult历史，保留attribution/confounders/data_failure/premature_evaluation语义。
6. 建立S09 LearningRecord等级、promotion_gate、新旧经验关系和真实写入边界。
7. 建立freshness/valid_until/data_window/经营状态可比性的复用门禁。
8. 明确Case只引用历史对象，不成为第二套事实源。

## 3. L3门禁
- [x] 模块定位与S02最小充分上下文一致。
- [x] 与S09 ValidationResult→LearningRecord链一致。
- [x] 统一scope替代所有历史强制product_id/ASIN。
- [x] Event→Decision→Task→ExecutionResult→Validation→Learning对象边界清楚。
- [x] 版本、supersedes、StrategyChain关系保留。
- [x] 失败记录不删除，不错误归因为策略失败。
- [x] freshness与复用门禁完整。
- [x] 与案例库保持单一事实源边界。
- [x] 不伪称memory_writes已written。
- [x] 静态示例覆盖旧历史、执行失败、策略替代、经验冲突、Case复用。
- [x] 当前无模块级阻塞。

## 4. 运行依赖
无新增编号；继续引用R06长期记忆数据库、R10 LearningWriteExecutor、R11 StrategyChain持久化。真实索引、版本、查询、freshness自动计算和写入回执不在Agent库实现。

## 5. 未宣称完成
当前通过L3框架静态验收，不代表长期数据库、历史召回、相似检索、memory write和StrategyChain持久化已真实运行。

## 6. 结论
`历史记忆/` 已达到当前Agent库框架阶段L3封板条件。下一模块进入 `工作流/` 启动分析。