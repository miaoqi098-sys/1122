# 技能模块

本目录是 Agent-1 唯一正式技能承载目录。旧 `Skill/` 目录中的定义已经合并到这里，后续不再维护两套技能体系。

## 技能模块的定位
技能模块是 Agent-1 可以在运行过程中重复调用的标准能力。它不同于经营知识：
- 经营知识库告诉 Agent-1“应该知道什么、如何理解问题”。
- 技能模块告诉 Agent-1“面对任务具体执行哪套标准处理能力”。
- 工具层提供技能执行过程中真正访问数据、系统和外部能力的工具。

因此：知识 ≠ 技能 ≠ 工具。

## V1.0 正式技能命名

以下十个名称确定为 Agent-1 V1.0 正式 Skill 名称，后续目录、配置、运行日志、测试和代码统一使用这些名称，不再使用旧称。

| 技能编号 | 正式名称 | 代码名 | 核心作用 | 当前状态 |
|---|---|---|---|---|
| S01 | 事件校验 | `EventValidation` | 校验智能事件包完整性、来源、字段、对象和基本合法性 | V1.0规则与接口骨架已建立 |
| S02 | 上下文装载 | `ContextLoading` | 根据当前问题选择并装载产品历史、状态、指标、活动、风险、知识和相关专业结论 | V1.0规则与接口骨架已建立 |
| S03 | 冲突检测 | `ConflictDetection` | 识别多 Agent 结论、事实、目标、任务和策略之间的冲突 | 待实现 |
| S04 | 决策排序 | `DecisionPrioritization` | 根据目标优先级、经营状态、证据和硬约束确定问题及方案处理顺序 | 待实现 |
| S05 | 方案生成 | `OptionGeneration` | 基于事实、假设、经营知识和专业分析生成多个可执行候选方案 | 待实现 |
| S06 | 风险评估 | `RiskAssessment` | 判断方案风险来源、影响范围、可逆性、风险等级和审批要求 | 待实现 |
| S07 | 任务编排 | `TaskOrchestration` | 将总控决策转化为任务、责任对象、依赖顺序、观察窗口、成功标准、止损和回滚要求 | 待实现 |
| S08 | 结果验证 | `OutcomeValidation` | 判断任务执行后是否真正产生预期经营效果，而不仅是动作完成 | 待实现 |
| S09 | 经验沉淀 | `LearningWriteback` | 将成功经验、失败原因、适用边界和新知识写回长期记忆与案例 | 待实现 |
| S10 | 策略防抖 | `StrategyStabilization` | 防止观察窗口未结束、证据不足或短期波动导致频繁反向修改策略 | 待实现 |

## 正式目录命名
```text
技能模块/
├── S01_事件校验/
├── S02_上下文装载/
├── S03_冲突检测/
├── S04_决策排序/
├── S05_方案生成/
├── S06_风险评估/
├── S07_任务编排/
├── S08_结果验证/
├── S09_经验沉淀/
└── S10_策略防抖/
```

GitHub 不保留空目录，因此只有进入实际建设的 Skill 才创建实体目录；未建设 Skill 先在本框架中保留正式命名，不创建空占位目录。

## 旧名称迁移
以下旧称停止作为正式名称使用：
- S05 `候选方案生成` → `方案生成`
- S06 `风险分级` → `风险评估`
- S07 `任务拆解` → `任务编排`
- S09 `复盘写回` → `经验沉淀`

S01、S02、S03、S04、S08、S10 名称保持不变。

## 技能调用链
典型总控任务并不是只调用一个技能，而是按需要组合：

```text
收到智能事件包
↓
S01 事件校验
↓
S02 上下文装载
↓
匹配经营状态、指标和经营知识
↓
S03 冲突检测
↓
S04 决策排序
↓
S05 方案生成
↓
S06 风险评估
↓
形成总控决策
↓
S07 任务编排
↓
执行
↓
S08 结果验证
↓
S09 经验沉淀
```

S10 策略防抖贯穿计划、执行、观察和验证阶段。

## 每个正式技能的标准结构
每个技能建立独立目录后，至少包含：
1. `README.md`：技能目的、适用场景、边界和调用条件。
2. `输入规范.json`：技能需要的数据字段。
3. `输出规范.json`：技能必须返回的结构化结果。
4. `判断规则.md`：技能内部业务规则和处理流程。
5. `异常处理.md`：缺数据、数据冲突、调用失败等处理方式。
6. `示例/`：真实输入输出示例。
7. `测试/`：验收与边界测试。
8. `执行程序/`：后续真正可运行的程序实现。

## 技能与其他模块的关系
### 与大脑
大脑负责推理和方案比较；技能负责把高频处理过程标准化，减少每次从零推理。

### 与经营知识库
技能可以检索经营知识库。例如 S05 方案生成可以根据异常诊断卡和场景知识生成候选动作。

### 与经营状态识别和经营指标库
S02、S04、S05等技能需要读取经营状态和相关指标，不能脱离产品阶段直接运行。

### 与协作机制
S03、S07等技能需要知道不同 Agent 的职责和能力边界。

### 与工具层
技能本身定义“需要做什么”，工具层提供“实际怎么获取数据、怎么执行动作”。

### 与自主循环
观察、思考、计划、行动、验证、学习是 Agent-1 的运行循环；技能是循环各阶段可以调用的标准能力。

## 建设原则
- 一个技能解决一种可重复能力，不把整个 Agent-1 写成一个超级技能。
- 技能优先结构化输入和输出，便于 Agent运行器调用。
- 业务阈值不应散落硬编码在技能文档中，优先从经营指标库和配置读取。
- 技能必须允许“信息不足”作为合法输出，并指出需要补什么数据。
- 高风险技能输出必须经过权限矩阵和审批机制。
- 技能实现后必须有测试，不能只存在说明文档。

## 当前建设状态
- S01 已完成 V1.0 规则与接口骨架。
- S02 已完成 V1.0 规则与接口骨架，包含十个上下文域、输入/输出规范、判断规则、异常处理、上下文域映射、示例、测试与执行程序边界。
- S01/S02 尚未接入真实 Agent运行器代码。

下一阶段按顺序建设 S03 冲突检测，不同时铺开多个空技能。