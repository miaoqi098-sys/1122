# S04 判断规则

排序链：硬约束检查 → 目标层级 → 状态修正 → 风险/机会时间窗口 → 依赖 → 可逆性 → 证据 → 资源。

- 安全与可售问题默认高于增长任务。
- 现金、库存、最低利润等经营底线内部按当前状态动态排序。
- 同一层级不能仅凭一个指标决定先后。
- 风险即将发生时提高优先级；机会窗口即将关闭时也可提高，但不得突破上层约束。
- 阻塞其他多个任务的前置问题应提高排序。
- 证据不足且动作不可逆时，不因潜在收益高而直接排P0。
- 已进入观察窗口的策略默认不重复生成同级反向任务。
- 每次动态提高/降低目标权重必须记录原因。