# 任务、执行、验证、学习链示例

> 承接 `完整决策链.example.md` 中 `DEC-20260829-0001`。

## 1. S07 TaskOrchestration
输入：`decision_id = DEC-20260829-0001`

形成：
- `task_plan_id = TP-20260829-0001`
- `task_id = TASK-20260829-0001`：调整关键词竞价到决策允许范围
- `task_id = TASK-20260829-0002`：设置观察窗口和验证节点

Task保留：decision_id、strategy_chain_id、required_controls、success criteria、观察窗口和依赖关系。

此时只能说明“任务已创建/待执行”，不能写成“竞价已修改成功”。

## 2. 审批/调度/执行
假设该动作在当前权限内允许执行。

未来Scheduler/Executor执行后返回真实回执。

经ExecutionResultNormalizer形成：
`execution_result_id = EXE-20260829-0001`

示例事实：
- status = succeeded
- actual_changes = 关键词竞价从1.20调整到0.95
- source_receipt_ref = ADS-RECEIPT-001

这只证明API动作成功，不证明经营结果成功。

## 3. S08 OutcomeValidation
观察窗口结束后，读取真实数据。

形成：
`validation_id = VAL-20260831-0001`

示例：
- 新增点击：24
- 新增订单：2
- CPC下降
- ACOS回到目标允许区间
- 未触发库存/利润guardrail

结果：`success_with_caveats`

如果观察窗口未结束，则应输出继续观察，而不是提前生成此结论。

## 4. S09 LearningWriteback
形成：
`learning_id = LRN-20260831-0001`

LearningRecord示例结论：
- 在当前scope、Listing版本和活动背景下，适度降竞价优于立即暂停；
- 该结论只适用于本次证据环境，不自动升级为“所有61点击0订单都降价”的永久规则；
- 标记validity/freshness和未来复核条件。

同时生成memory_writes plan。

## 5. 真实长期记忆写入
没有LearningWriteExecutor回执前：
- 可以写“memory_writes plan已生成”；
- 不得写“长期记忆已written”。

未来R10成功回执后才能记录真实写入结果。

## 6. 完整追溯链
`EVT-20260829-0001 → DI-20260829-0001 → OPT-B → DEC-20260829-0001 → TP-20260829-0001 → TASK-20260829-0001 → EXE-20260829-0001 → VAL-20260831-0001 → LRN-20260831-0001`

`strategy_chain_id = STR-MAHJONG-ADS-001`贯穿策略生命周期。