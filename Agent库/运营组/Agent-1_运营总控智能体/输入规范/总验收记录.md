# Agent-1 输入规范总验收记录

## 1. 验收范围
- README与输入来源边界；
- 标准智能事件包Schema；
- 专业Agent结构化结果Schema；
- 人工与系统输入；
- 统一引用关系；
- Event与ContextPackage边界；
- 多来源示例与静态验收。

## 2. 关键修复
1. 修复旧事件Schema强制`recommendation`的问题，与S01“缺失仅警告”一致。
2. 修复旧Schema强制`product_id`的问题，支持account/store/global作用域。
3. 增加`scope_type/scope_id/scope_objects`与来源类型。
4. 区分标准Event、专业Agent结果、人工输入、系统输入四类正式输入。
5. 建立request/source/evidence/parent/related/time等追溯关系。
6. 明确S02只能消费S01.normalized_event，ContextPackage保持唯一事实源。

## 3. L3门禁
- [x] 模块定位与Agent-1输入职责一致。
- [x] README与实际文件一致。
- [x] Event多作用域与S01 V1.1一致。
- [x] recommendation非硬必填。
- [x] 专业Agent结果与协作机制请求/返回语义一致。
- [x] 人工偏好/授权与客观事实分离。
- [x] 系统失败不被解释为业务指标0。
- [x] 业务主键与run_id/trace_id运行追踪分离。
- [x] Event→S01→S02→ContextPackage边界清楚。
- [x] 有多来源静态示例与验收。
- [x] 未混入真实消息队列、鉴权、Runner或数据接入实现。
- [x] 当前无模块级阻塞。

## 4. 运行依赖
无新增独立编号；继续引用R01 Skill Runner、R02 Agent Runner、R03 Schema Validator及未来系统/数据接口层来源鉴权能力。

## 5. 未宣称完成
当前只通过框架L3静态验收，不代表真实事件接入、消息路由、Schema机器验证、鉴权和上下文检索已经运行。

## 6. 结论
`输入规范/` 已达到当前Agent库框架阶段L3封板条件。下一模块进入 `输出规范/` 启动分析；若后续发现FinalDecision/Task输出对象与本模块引用语义冲突，按真实归属生成修订任务。