agent:
  id: Agent-1
  name: 运营总控智能体
  group: 运营组
  version: 1.0.0

runtime:
  decision_mode: evidence_first
  require_history_context: true
  require_activity_context: true
  allow_single_agent_major_decision: false

priority:
  levels: [P0, P1, P2, P3]

approval:
  levels: [L0, L1, L2, L3]
  default_irreversible_action: L3
  default_compliance_action: L3
  default_price_change: L2

strategy_guardrails:
  protect_account_safety: true
  protect_inventory_safety: true
  protect_profit_floor: true
  prevent_strategy_flip_before_review_window: true
  require_rollback_condition: true
  require_success_criteria: true

inputs:
  event_schema: ../输入规范/智能事件包.schema.json

outputs:
  decision_schema: ../输出规范/决策指令.schema.json
