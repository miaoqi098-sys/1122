# Agent-2｜L4 全局一致性总验收记录

## 1. 验收目标
确认Agent-2已经从单页角色说明形成可与Agent-1对接的商品状态专业Agent框架，并且不越权成为第二个经营决策中心。

## 2. 身份与职责
- [x] Agent-2定位为商品/链接事实状态监控与变化检测专业Agent。
- [x] Agent-1仍为唯一最终经营决策出口。
- [x] Agent-2默认只读、分析、上报与请求补证据，不直接执行价格/广告/Listing/库存/促销/合规动作。
- [x] 与Agent-7/9/10/11/12等相邻专业Agent边界明确。

## 3. 两条正式输出
- [x] 主动状态变化可映射为Agent-1标准智能事件包。
- [x] Agent-1定向请求可返回标准专业Agent结果。
- [x] recommendation保持可选来源意见，不是FinalDecision。
- [x] Agent-2不会生成FinalDecision或Task执行结果。

## 4. 监控与状态模型
- [x] 可售/页面/Featured Offer/价格展示/Listing内容/变体/评分/促销展示/资质限制等核心状态域已覆盖。
- [x] `unknown`是合法状态，不要求模型猜值。
- [x] 已定义Agent-2内部状态快照，并提供`商品状态快照.schema.json`。
- [x] 状态快照不替代Agent-1 ContextPackage。

## 5. 变化检测与事件治理
- [x] 已定义当前快照→历史快照diff→持续确认→严重度建议→去重→Event流程。
- [x] 高影响明确变化和易抖动页面变化采取不同确认策略。
- [x] 事件去重指纹和恢复事件规则存在。
- [x] Event severity与Agent-1 S04 business_priority分离。
- [x] 工具失败与业务异常严格分离。
- [x] 前后台来源冲突保留双证据而不是互相覆盖。

## 6. 历史与证据
- [x] 状态快照、未关闭事件、first/last seen、occurrence、恢复关系等历史语义已定义。
- [x] source/evidence/freshness/confidence可追溯。
- [x] 真实持久化明确归R06，不在Agent-2内伪造数据库状态。

## 7. 数据与运行依赖
- [x] 官方后台/SP-API需求归R04。
- [x] Agent Runner/Schema Validator/Scheduler/历史存储分别复用R02/R03/R07/R06。
- [x] 新发现的前台商品页状态采集属于独立共享能力，已新增R12。
- [x] R12与R04边界清楚：后台官方事实与真实前台呈现不能相互替代。
- [x] Agent-2不实现API、页面抓取器、数据库或定时器。

## 8. 配置与机器合同
- [x] 已提供`agent-2.config.yaml`，引用Agent-1 Event/专业结果合同和Agent-2快照Schema。
- [x] 配置明确tool failure不是business event。
- [x] P0-P3只做事件severity，跨Agent business priority仍由Agent-1 S04形成。

## 9. 测试与示例
- [x] 静态测试覆盖页面变狗、Featured Offer抖动、suppressed、前后台冲突、划线价、评分、资质、去重、恢复、专业请求、scope和工具失败。
- [x] 动态SP-API/前台采集/Scheduler/存储/Event投递/Agent Runner测试明确未执行。
- [x] 已提供标准商品状态Event示例和专业Agent请求响应示例。

## 10. 阻塞
当前无Agent-2框架阻塞。

动态运行能力缺失属于R02/R03/R04/R06/R07/R12，不阻塞框架层L4。

## 11. 结论
**Agent-2 商品状态智能体 V1.x 框架层 L4通过。**

Agent-2现在可以作为后续运行层实现和Agent-1协作的稳定专业接口基线；动态监控尚未真实运行、未验收。下一步进入Agent-3真实目录启动分析。