# Agent-1 动态运行测试挂账

> 本文件只列未来需要真实运行能力才能执行的测试。当前状态统一为 `BLOCKED_RUNTIME`，不得标记PASS。

## D01｜Skill Runner主链路
验证S01-S09按路由执行，S10可横向介入。
依赖：R01。
状态：BLOCKED_RUNTIME。

## D02｜Agent Runner上下文与协作
验证Agent-1与专业Agent请求/返回、上下文装载和错误恢复。
依赖：R02。
状态：BLOCKED_RUNTIME。

## D03｜Schema运行校验
验证Event、DecisionItem、Option、RiskAssessment、FinalDecision、Task、ExecutionResult、ValidationResult、LearningRecord等对象运行时校验与错误回传。
依赖：R03。
状态：BLOCKED_RUNTIME。

## D04｜Amazon数据接口
验证SP-API/Ads API真实数据、新鲜度、失败降级与权限。
依赖：R04/R05。
状态：BLOCKED_RUNTIME。

## D05｜长期记忆与案例检索
验证历史、Case、LearningRecord持久化、索引、版本、新鲜度与相似案例候选检索。
依赖：R06。
状态：BLOCKED_RUNTIME。

## D06｜Scheduler
验证TaskGraph依赖/互斥/定时/观察窗口/重试/断点。
依赖：R07。
状态：BLOCKED_RUNTIME。

## D07｜Executor
验证审批后真实执行、幂等、权限、失败、rollback Task。
依赖：R08。
状态：BLOCKED_RUNTIME。

## D08｜ExecutionResultNormalizer
验证不同API/人工回执转换为canonical ExecutionResult并保持业务引用。
依赖：R09。
状态：BLOCKED_RUNTIME。

## D09｜LearningWriteExecutor
验证memory_writes plan真实写入、失败回执与幂等。
依赖：R10。
状态：BLOCKED_RUNTIME。

## D10｜StrategyChain持久化
验证策略链版本、merge/override、反转次数、并发更新和历史读取。
依赖：R11。
状态：BLOCKED_RUNTIME。

## D11｜断点恢复与重入
模拟系统重启、审批晚到、观察窗口结束和新证据到达，从resume_from恢复而不复制业务对象。
依赖：R01/R02/R06/R07/R11。
状态：BLOCKED_RUNTIME。

## D12｜并发与幂等
验证同一Event重复投递、同一approval回执重复消费、同一Task重试不会生成重复Decision/Task/LearningRecord。
依赖：未来运行层完整编排。
状态：BLOCKED_RUNTIME。

## 原则
这些测试未执行不是Agent库框架失败，也不能为了让测试变绿而把运行实现塞进Agent-1。未来基础设施完成后再升级状态。