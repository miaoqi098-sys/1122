# Agent-1 配置总验收记录

## 1. 验收范围
覆盖：`README.md`、`配置治理.md`、`agent-1.config.yaml`、`示例与验收.md`。

## 2. L3门禁结果
- [x] 配置定位清晰，不与决策规则、目标系统、指标库、Skill接口重复。
- [x] 配置只保存开关、引用、版本、覆盖合同，不成为第二套业务规则真源。
- [x] Event与FinalDecision均引用canonical Schema。
- [x] deprecated `决策指令.schema.json` 不再作为新输出配置。
- [x] 旧固定L0-L3审批默认语义已移除。
- [x] P0-P3仅用于event severity等已有层级，不覆盖S04动态business_priority。
- [x] 覆盖顺序、不可覆盖边界、版本与兼容性已定义。
- [x] 业务阈值不散落硬编码在Agent根配置。
- [x] 真实加载器、环境变量、密钥、热更新与运行时校验明确属于未来运行层。
- [x] 静态示例与反例完整。

## 3. 关键修复
- `outputs.decision_schema`旧引用迁移为`schemas.final_decision: ../输出规范/FinalDecision.schema.json`。
- 移除`approval.levels`与固定动作→Lx默认等级，改为布尔guardrail与正式approval语义引用。
- `priority.levels`迁移为`priority_semantics.event_severity_levels`，明确business priority由S04产生。
- 增加config_version、compatible_agent_version、rule_refs、override_policy和runtime_placeholders。

## 4. 运行依赖
无新增编号。继续引用R03配置/Schema运行时验证、R07调度、R08执行、R10学习写入、R11策略链持久化等既有能力。

## 5. 结论
`Agent-1-配置-总验收-1`：**L3静态门禁通过，配置模块框架封板。**

不代表真实配置中心、动态加载和运行时覆盖验证已实现。