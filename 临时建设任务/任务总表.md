# Agent库临时建设任务总表

> Agent库建设唯一总队列与当前施工指针。所有新增、阻塞、完成、回顾和下一任务都必须回写本表。

## 1. 当前施工指针
- 当前 Agent：`Agent-1_运营总控智能体`
- 当前一级模块：`输出规范`
- 当前任务：`Agent-1-输出规范-启动分析-1`
- 当前状态：`🟦 执行中`
- 当前回顾层级：输入规范 L3 已通过；输出规范已完成现状分析并展开确认任务
- 当前任务指纹：`Agent-1|输出规范|启动分析|FinalDecision Task 审批阻断追溯`

## 2. 已封板模块
- 决策模板库｜✅ L3框架封板
- 案例库｜✅ L3框架封板
- 工具与数据需求｜✅ L3框架封板
- 决策规则｜✅ L3框架封板
- 输入规范｜✅ L3框架封板

## 3. 输出规范启动分析结论
现状：正式目录只有旧 `决策指令.schema.json`。旧Schema把 `product_id` 强制为所有输出作用域、把 `tasks[]` 嵌入决策对象，并使用旧 `priority/decision/approval_level` 字段，已经落后于 canonical `DecisionSelector.output → FinalDecision → S07 TaskPlan/TaskGraph` 链。

已确认冲突：
- 输出Schema与 DecisionSelector.output 的 `decision_item_id/business_priority/selected_option/risk_assessment/decision_basis/approval/strategy_chain_id` 不一致；
- 强制 `product_id` 与统一 scope 不一致；
- 把 Task 直接内嵌在决策输出，越过 S07 TaskOrchestration；
- 缺少 `next_action` / approval / blocked / more_evidence 等非正常路由；
- 缺少 Event→DecisionItem→Decision→TaskPlan→Task 的统一追溯说明。

确认任务：
- `Agent-1-输出规范-启动分析-1`｜🟦 执行中｜指纹：`Agent-1|输出规范|启动分析|FinalDecision Task 审批阻断追溯`
- `Agent-1-输出规范-README与输出边界-1`｜⬜ 待执行｜指纹：`Agent-1|输出规范|模块治理|DecisionSelector FinalDecision S07边界`
- `Agent-1-输出规范-FinalDecision-1`｜⬜ 待执行｜指纹：`Agent-1|输出规范|Schema迁移|旧决策指令到FinalDecision`
- `Agent-1-输出规范-Task引用-1`｜⬜ 待执行｜指纹：`Agent-1|输出规范|引用关系|FinalDecision到TaskPlan Task`
- `Agent-1-输出规范-审批阻断与下一动作-1`｜⬜ 待执行｜指纹：`Agent-1|输出规范|异常路由|approval blocked more_evidence`
- `Agent-1-输出规范-追溯字段-1`｜⬜ 待执行｜指纹：`Agent-1|输出规范|追溯|Event Decision Task链`
- `Agent-1-输出规范-示例与验收-1`｜⬜ 待执行｜指纹：`Agent-1|输出规范|静态验收|FinalDecision与S07边界`
- `Agent-1-输出规范-总验收-1`｜⬜ 待执行｜指纹：`Agent-1|输出规范|L3验收|输出模块一致性`

无新增阻塞；旧Schema为局部可逆迁移，可无人值守修订。

## 4. 后续模块
`输出规范 → 事件处理 → 任务调度 → 历史记忆 → 工作流 → 自主循环 → 配置 → 日志规范 → 测试 → 示例 → README总同步 → L4总回顾`

## 5. 当前下一可执行任务
`Agent-1-输出规范-启动分析-1`。