# Agent库临时建设任务总表

> Agent库建设唯一总队列与当前施工指针。所有新增、阻塞、完成、回顾和下一任务都必须回写本表。

## 1. 当前施工指针
- 当前 Agent：`Agent-1_运营总控智能体`
- 当前一级模块：`输入规范`
- 当前任务：`Agent-1-输入规范-启动分析-1`
- 当前状态：`🟦 执行中`
- 当前回顾层级：决策规则 L3 已通过；输入规范已完成现状分析并展开确认任务
- 当前任务指纹：`Agent-1|输入规范|启动分析|Event ContextPackage 专业Agent 人工系统输入`

## 2. 已封板模块
- 决策模板库｜✅ L3框架封板
- 案例库｜✅ L3框架封板
- 工具与数据需求｜✅ L3框架封板
- 决策规则｜✅ L3框架封板

## 3. 输入规范启动分析结论
现状：正式目录只有 `智能事件包.schema.json`。该旧Schema强制 `product_id` 和 `recommendation`，只允许 Agent-2至13，缺少 S01 V1.1 的 account/store/global 作用域、scope_objects、warnings/duplicate signal语义，也没有专业Agent结构化结果、人工输入、系统输入、统一引用关系和输入来源边界说明。

已确认的主要冲突：
- `recommendation` 在旧Schema中为 required，但 S01 已明确其缺失只属于非阻断警告；
- `product_id` 被强制 required，与 account/store/global 作用域允许 null 冲突；
- 缺少 `scope_type/scope_id/scope_objects`，无法承载统一作用域；
- 旧Schema把“Agent事件”当作 Agent-1 所有输入，无法表达协作返回、人工指令、系统/监控事件等不同输入来源。

确认任务：
- `Agent-1-输入规范-启动分析-1`｜🟦 执行中｜指纹：`Agent-1|输入规范|启动分析|Event ContextPackage 专业Agent 人工系统输入`
- `Agent-1-输入规范-README与输入边界-1`｜⬜ 待执行｜指纹：`Agent-1|输入规范|模块治理|输入来源与S01S02边界`
- `Agent-1-输入规范-智能事件包-1`｜⬜ 待执行｜指纹：`Agent-1|输入规范|Schema冲突|S01 V1.1 Event`
- `Agent-1-输入规范-专业Agent结果-1`｜⬜ 待执行｜指纹：`Agent-1|输入规范|协作输入|专业Agent结构化返回`
- `Agent-1-输入规范-人工与系统输入-1`｜⬜ 待执行｜指纹：`Agent-1|输入规范|来源分类|人工与系统事件`
- `Agent-1-输入规范-统一引用关系-1`｜⬜ 待执行｜指纹：`Agent-1|输入规范|追溯|source trace parent refs`
- `Agent-1-输入规范-ContextPackage边界-1`｜⬜ 待执行｜指纹：`Agent-1|输入规范|上下文边界|S01 normalized_event 到 S02 ContextPackage`
- `Agent-1-输入规范-示例与验收-1`｜⬜ 待执行｜指纹：`Agent-1|输入规范|静态验收|多作用域多来源输入`
- `Agent-1-输入规范-总验收-1`｜⬜ 待执行｜指纹：`Agent-1|输入规范|L3验收|输入模块一致性`

无新增阻塞任务；旧Schema属于局部可逆修订，可无人值守修复。

## 4. 后续模块
`输入规范 → 输出规范 → 事件处理 → 任务调度 → 历史记忆 → 工作流 → 自主循环 → 配置 → 日志规范 → 测试 → 示例 → README总同步 → L4总回顾`

## 5. 当前下一可执行任务
`Agent-1-输入规范-启动分析-1`。