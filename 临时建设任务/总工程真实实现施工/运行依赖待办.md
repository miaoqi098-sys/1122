# 总工程真实实现运行依赖待办

## 当前依赖

### R-REAL-001｜Amazon只读授权
- 阶段：P3 / 后续生产验收
- 需要：SP-API / Ads API 只读授权与安全Secret引用方式
- 当前状态：待后续真实接入
- 当前影响：**不阻塞 `P3_STAGE_MACHINE_VERIFIED`**；继续阻塞真实 Amazon `AUTH_VERIFIED / READ_VERIFIED / LIVE_DATA_VERIFIED`。
- P3 已具备：SP-API/Ads API真实HTTP适配代码、LWA刷新、SecretRef边界、限流与有界重试；机器验收全部使用 Fake/Mock，不调用生产。
- 原则：Secret明文不得进入Git、Prompt、普通日志或本文件。

### R-REAL-002｜持久化技术与数据库
- 阶段：P2/P4
- 需要：ProductIdentity与MetricSnapshot长期持久化
- 当前状态：P2 已采用 `InMemoryProductIdentityStore` 冻结 Store / Resolution Contract，数据库选型延后。
- 当前影响：不阻塞 P2 MACHINE_VERIFIED；进入 P4 事实层后先以 Canonical Contract 为准决定最小实现，只有确需长期/跨进程持久化时再确定数据库/ORM。

### R-REAL-003｜部署环境
- 阶段：P8前
- 需要：可部署的测试/验收环境、版本证据与健康检查
- 当前状态：待定
- 当前影响：不阻塞当前代码施工；会阻塞最终 RUNTIME_VERIFIED。

### R-REAL-004｜首个验收seller/marketplace/product
- 阶段：P3/P4
- 需要：用于真实只读验证的账号、站点与product范围
- 当前状态：尚未绑定
- 当前影响：不阻塞 P3 离线阶段机器验收，也不阻塞 P4 Schema/事实层离线施工；会阻塞真实业务读取与 live snapshot 验收。

### R-REAL-005｜机器CI执行入口
- 阶段：P1/P2/P3
- 当前状态：✅ 已解决
- P1 workflow：`real-v1-p1-ci`
- P2 workflow：`real-v1-p2-ci`
- P3 workflow：`real-v1-p3-ci`
- P3阶段机器PASS run：`33305719435`（48 passed）
- 同一 head P1 run：`33305719469` success
- 同一 head P2 run：`33305719437` success
- Secret要求：CI不得注入任何生产Secret。

## 当前阶段提醒

P3 Amazon只读连接已完成离线阶段机器验收：`P3_STAGE_MACHINE_VERIFIED`。

仍不得宣称：
- `AUTH_VERIFIED`
- `READ_VERIFIED`
- `LIVE_DATA_VERIFIED`

下一候选阶段为 P4 MetricSnapshot事实层；在任务总表正式移动指针前不提前创建 P4 空目录。
