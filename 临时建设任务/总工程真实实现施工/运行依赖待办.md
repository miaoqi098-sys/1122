# 总工程真实实现运行依赖待办

## 当前依赖

### R-REAL-001｜Amazon只读授权
- 阶段：P3
- 需要：SP-API / Ads API 只读授权与安全Secret引用方式
- 当前状态：待后续接入
- 当前影响：不阻塞 P3 Connector 接口、适配器、Mock 与安全测试施工；会阻塞真实 Amazon `AUTH_VERIFIED / READ_VERIFIED / LIVE_DATA_VERIFIED`。
- 原则：Secret明文不得进入Git、Prompt、普通日志或本文件。

### R-REAL-002｜持久化技术与数据库
- 阶段：P2/P4
- 需要：ProductIdentity与MetricSnapshot长期持久化
- 当前状态：P2 已采用 `InMemoryProductIdentityStore` 冻结 Store / Resolution Contract，数据库选型延后。
- 当前影响：不阻塞 P2 MACHINE_VERIFIED；进入长期运行、跨进程共享或 P4 事实层持久化时重新评估。

### R-REAL-003｜部署环境
- 阶段：P8前
- 需要：可部署的测试/验收环境、版本证据与健康检查
- 当前状态：待定
- 当前影响：不阻塞当前代码施工；会阻塞最终 RUNTIME_VERIFIED。

### R-REAL-004｜首个验收seller/marketplace/product
- 阶段：P3/P4
- 需要：用于真实只读验证的账号、站点与product范围
- 当前状态：尚未绑定
- 当前影响：不阻塞 dummy/mock 合同测试；会阻塞真实业务读取验收。

### R-REAL-005｜机器CI执行入口
- 阶段：P1/P2
- 当前状态：✅ 已解决
- P1 workflow：`real-v1-p1-ci`
- P1机器PASS run：`33301502753`
- P2 workflow：`real-v1-p2-ci`
- P2机器PASS run：`33302190845`
- Secret要求：CI不得注入任何生产Secret。

## 当前阶段提醒

施工指针已进入 P3 Amazon只读Connector。

P3可以先完成：
- Connector Contract；
- read-only operation allowlist；
- 请求/响应适配器；
- SecretRef 接口对接；
- Dummy/Fake Transport；
- 超时/认证失败/限流/重试边界；
- pytest + CI。

在没有真实授权前不得把 Mock PASS 冒充 `AUTH_VERIFIED / READ_VERIFIED / LIVE_DATA_VERIFIED`。
