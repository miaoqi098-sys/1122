# 总工程蓝图自动施工运行依赖待办

> 本文件只记录当前静态框架未来落地所需的真实运行依赖；不在本自动施工阶段实现。

## R-GP-001｜ProductIdentity 持久化与唯一性
- 未来归属：数据系统 / 系统运行层
- 需求：持久化 ProductIdentity；保证 `product_id` 唯一；保证 active 身份唯一性约束。
- 当前是否阻塞：否。

## R-GP-002｜IdentityResolution 服务
- 未来归属：数据系统 / 对外连接 / 任务中心
- 需求：处理外部数据无法唯一映射 product_id、多候选、ASIN/SKU 冲突、身份迁移。
- 当前是否阻塞：否。

## R-GP-003｜外部标识映射采集
- 未来归属：对外连接
- 需求：通过 Amazon SP-API、报表或文件导入建立 ASIN/SKU/FNSKU 与内部 product_id 的真实映射。
- 当前是否阻塞：否。

## R-GP-004｜MetricSnapshot 采集与持久化
- 未来归属：数据系统 / 对外连接
- 需求：从 SP-API、Ads API、文件和成本配置生成并持久化按 product_id 对齐的事实快照；保留来源与修订历史。
- 当前是否阻塞：否。

## R-GP-005｜统一业务时间窗口与新鲜度判定
- 未来归属：数据系统 / 系统公共层
- 需求：按 marketplace_timezone 计算 business_date/window，维护数据源SLA，生成 freshness 状态。
- 当前是否阻塞：否。

## R-GP-006｜指标字典与计算注册表
- 未来归属：数据系统 / 业务系统
- 需求：维护 metric_code、单位、公式、版本、可聚合规则和 calculation_ref。
- 当前是否阻塞：否。

## R-GP-007｜利润计算服务与成本版本
- 未来归属：数据系统 / 业务系统/利润与经营
- 需求：统一收入与成本口径；输出成本版本、完整度和缺失成本项。
- 当前是否阻塞：否。

## R-GP-008｜BusinessState Engine
- 未来归属：业务系统 / 数据系统 / 系统运行层
- 需求：读取 MetricSnapshot/Event/验证证据，按照版本化状态评估规则生成维度状态与 overall_status，并保留历史状态链。
- 当前是否阻塞：否。

## R-GP-009｜产品经营阶段定义字典
- 未来归属：业务系统/运营/产品
- 需求：由业务架构正式确认 stage_code、名称、定义、进入条件、退出条件、最短观察窗口和版本策略。
- 当前是否阻塞：否（不阻塞Contract，但阻塞真实阶段判定）。

## R-GP-010｜ProductStage 判定与人工覆盖工作流
- 未来归属：业务系统 / 系统运行层 / 任务中心
- 需求：执行阶段规则、生成历史阶段链；人工覆盖必须记录身份、原因、时效并形成可审计活动。
- 当前是否阻塞：否。

## R-GP-011｜Goal Repository 与生命周期服务
- 未来归属：业务系统 / 系统运行层
- 需求：持久化 Goal，支持状态转换、版本历史、superseded 链与产品当前 primary Goal 查询。
- 当前是否阻塞：否。

## R-GP-012｜Goal 评估器
- 未来归属：业务系统 / 数据系统 / 验证系统
- 需求：读取 MetricSnapshot/ValidationResult，根据 success_criteria 和 observation_window 判断目标进展。
- 当前是否阻塞：否。

## R-GP-013｜目标切换审计与业务活动
- 未来归属：系统运行层 / AgentActivity / 记忆系统
- 需求：切换主目标时保留旧Goal并形成审计活动。
- 当前是否阻塞：否。

## R-GP-014｜Event Normalizer / Repository / Signal Detector
- 未来归属：系统运行层 / 数据系统 / Agent协作
- 需求：把专业 Domain Event 规范化为 Canonical Event；持久化、去重、关联、状态推进；从数据变化生成候选 Signal。
- 当前是否阻塞：否。

## R-GP-015｜TaskCenter Repository 与事务状态机
- 未来归属：任务中心 / 系统运行层
- 需求：持久化 Task/Approval/HumanActionRequest，执行合法状态迁移、依赖检查、查询、审计。
- 当前是否阻塞：否。

## R-GP-016｜Policy / Permission 与审批运行服务
- 未来归属：政策边界 / 系统公共层 / 任务中心
- 需求：真实权限判定、required_controls 检查、审批动作、审批身份与审计。
- 当前是否阻塞：否。

## R-GP-017｜Executor 与 ExecutionResult Normalizer
- 未来归属：执行系统
- 需求：受控执行真实动作、保存执行前状态、产生 Action/ExecutionResult、支持失败/部分成功/回滚语义。
- 当前是否阻塞：否。

## R-GP-018｜Validation Service
- 未来归属：执行系统 / 业务系统 / 数据系统
- 需求：根据 Task/Goal/Event 的验证窗口生成 ValidationResult；执行成功不能替代验证。
- 当前是否阻塞：否。

## R-GP-019｜AgentActivity Generator / Repository
- 未来归属：系统运行层 / 系统公共层
- 需求：监听结构化业务对象变化生成业务可见活动；支持 importance/visibility、聚合去重和查询。
- 当前是否阻塞：否。

## R-GP-020｜Memory Repository / Timeline / StageSummary Generator
- 未来归属：记忆系统
- 需求：追加保存 Memory，建立检索/时间线；从真实对象引用生成可追溯阶段摘要。
- 当前是否阻塞：否。

## R-GP-021｜ProductStateAggregator 运行服务
- 未来归属：业务系统 / 系统运行层
- 需求：按 product_id 聚合状态、阶段、目标、任务、Action、Event、Memory并提供 freshness/degraded 读模型。
- 当前是否阻塞：否。

## R-GP-022｜HomeCommandCenterAggregator 运行服务
- 未来归属：系统运行层 / UI接口层
- 需求：聚合经营概览、HumanTasks、AgentActivity、ProductStatusCards、ImportantEvents；支持分区降级和缓存失效。
- 当前是否阻塞：否。

## R-GP-023｜Amazon SP-API / Ads API 真实接入
- 未来归属：对外连接
- 需求：完成认证、授权、限流、报表/实时数据获取；Ads 写权限与读取权限分离。
- 当前是否阻塞：否（阻塞真实MVP，不阻塞静态设计）。

## R-GP-024｜Agent Runtime / Scheduler / Queue
- 未来归属：系统运行层 / 调度与自动化层
- 需求：真实运行专业Agent与Agent-1，处理事件、任务唤醒、失败重试、幂等与上下文装载。
- 当前是否阻塞：否（阻塞真实MVP）。

## R-GP-025｜首页与产品状态卡真实接口
- 未来归属：UI / 系统运行层
- 需求：为 HomeCommandCenterView、ProductStatusCardView、HumanTaskView 提供受权限控制的真实读取接口和交互入口。
- 当前是否阻塞：否（阻塞真实UI）。

## R-GP-026｜真实单产品MVP验收环境
- 未来归属：测试与验收 / 对外连接 / 执行系统
- 需求：选择授权测试产品，接真实数据，完成只读链路后再在明确权限下测试一个受控广告人工审批动作；记录 LIVE_DATA_VERIFIED / BUSINESS_LOOP_VERIFIED 的机器可验证证据。
- 当前是否阻塞：否（当前静态工程已可继续）。
