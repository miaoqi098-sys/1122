# 05 事件与时间线

记录“什么时候发生了什么”，用于长期复盘经营动作与结果之间的关系。

## D1 对象

- `events`
- `decisions`
- `tasks`
- `validation_results`
- `data_layer_audit`

## 时间线目标

未来任意产品都可以重建：

```text
数据变化
→ Agent Event
→ A1 FinalDecision
→ Task
→ ExecutionResult
→ ValidationResult
→ 后续经营变化
```

事件、决策和验证结果原则上只新增，不静默删除。
