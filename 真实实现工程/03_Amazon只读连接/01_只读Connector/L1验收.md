# REAL-P3-01｜L1验收

## 验收结论

`PASS / CONNECTOR_CONTRACT_VERIFIED`

## 正式路径

`真实实现工程/03_Amazon只读连接/01_只读Connector/`

## 已实现

- SP-API / Ads API 内部只读 Operation Contract；
- 显式 read-only allowlist；
- 未列入 Operation 不开放写能力；
- `ConnectorRequest / ConnectorResponse / SourceTrace`；
- `ConnectorTransport` Protocol；
- `FakeTransport`，无网络 I/O；
- P1 `SecretRef` 引用对接；
- Transport 只拿 credential ref key，不暴露 Secret 明文；
- 认证失败立即 fail closed，不重试；
- rate-limit / timeout 有界重试，最多 5 次，默认 3 次；
- trace 保留 API family、operation、seller、marketplace、correlation id、transport request id、attempt count；
- P3 独立 GitHub Actions 机器门禁。

## 机器证据

- workflow：`real-v1-p3-ci`
- run id：`33303090625`
- job：`p3-amazon-read-connector-tests`
- implementation head at trigger：`7086d4b332237c28aa56149bf9092ca27dbeb129`
- PR merge test commit：`743c6c1fa6ba1b36b79c02aef42952fc9450207d`
- runner：Ubuntu 24.04 / Python 3.12.14
- 结果：`12 passed in 0.10s`
- conclusion：`success`

同一 implementation head 关联回归：
- P2 workflow run `33303090617`：success；
- P1 workflow run `33303090638`：success。

## 安全边界

- 无真实 Amazon credential；
- 无真实 SP-API / Ads API 网络调用；
- 无经营写能力；
- 无 Ads 写操作；
- 无自动审批；
- 无 Executor；
- Fake/CI PASS 不冒充真实 Amazon 授权或真实业务读取。

## 尚未达到

以下状态因缺少真实只读授权与验收 seller/marketplace/product，仍为 PENDING：
- `AUTH_VERIFIED`
- `READ_VERIFIED`
- `LIVE_DATA_VERIFIED`

对应依赖：`R-REAL-001`、`R-REAL-004`。

## L1结论

REAL-P3-01 已满足当前 Connector Contract 阶段的真实实现、测试、安全边界、Git证据和机器门禁。允许施工指针进入 P4 MetricSnapshot 事实层，以 Mock/fixture 数据继续构造事实 Contract；不得把 P3 的 Mock PASS 当成真实 Amazon 数据已经接通。
