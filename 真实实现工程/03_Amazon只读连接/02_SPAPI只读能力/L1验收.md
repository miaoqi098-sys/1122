# REAL-P3-02｜SP-API只读能力 L1验收

## 验收结论

`PASS / SPAPI_READ_CAPABILITY_VERIFIED`

## 正式路径

`真实实现工程/03_Amazon只读连接/02_SPAPI只读能力/`

## 已实现

- 真实可发起 HTTP GET 的 `HttpxHttpClient` 适配器；
- `SpApiReadTransport` 与 P3-01 `ConnectorTransport` Contract 对接；
- SP-API NA / EU / FE 官方 endpoint allowlist；
- marketplace → SP-API region 映射，未知 marketplace fail closed；
- `spapi.catalog.get_item` → Catalog Items v2022-04-01 GET；
- `spapi.listings.get_item` → Listings Items v2021-08-01 GET；
- `spapi.inventory.get_summaries` → FBA Inventory v1 GET；
- access token 每次请求通过 `SecretProvider + SecretRef` 临时 resolve，只进入 `x-amz-access-token` header；
- 解析 `x-amzn-RequestId`、`x-amzn-RateLimit-Limit` 与 HTTP status；
- 401 / 403 → `ConnectorAuthError`，不作为普通成功；
- 429 → `ConnectorRateLimitError`；
- timeout → `ConnectorTimeoutError`；
- 其他非 2xx → `SpApiHttpError`，异常不携带响应 body/Secret；
- SourceTrace 已扩展 request id / rate-limit / response status；
- P3-01 + P3-02 联合回归测试纳入同一 GitHub Actions 门禁。

## 机器证据

- workflow：`real-v1-p3-ci`
- run id：`33303938924`
- job：`p3-amazon-read-connector-tests`
- implementation head：`dc98587c1f28d59c11b0a1ea292cca89d5b45f67`
- PR merge test commit：`92c79470caafbd0e5f6c85ab6b975b7ed274dd8c`
- runner：Ubuntu 24.04 / Python 3.12.14
- 结果：`23 passed in 0.16s`
- conclusion：`success`

## 官方协议核验

本项施工时重新核验 Amazon SP-API 当前官方文档：
- SP-API endpoint 分 NA / EU / FE；
- LWA access token 通过 `x-amz-access-token`；
- 响应可提供 `x-amzn-RequestId` 与 `x-amzn-RateLimit-Limit`；
- 401/403/429 与当前错误映射一致。

## 安全边界

- pytest 使用 Fake HTTP client，不调用生产 Amazon；
- 无真实 Amazon credential；
- 无 Secret 写入 Git / trace / 普通日志；
- 仅实现 GET；
- 无 POST / PUT / PATCH / DELETE；
- 无 Ads 写能力；
- 无自动审批 / Executor。

## 尚未达到

- `AUTH_VERIFIED`：PENDING；
- `READ_VERIFIED`：PENDING；
- `LIVE_DATA_VERIFIED`：PENDING。

原因：仍缺真实 Amazon 只读授权与首个验收 seller / marketplace / product。

## L1结论

REAL-P3-02 已满足当前 SP-API 只读能力文件夹的真实实现、测试、安全边界、Git证据和机器门禁。允许指针移动到 `03_AdsAPI只读能力`，不得因此宣称生产 Amazon 已接通。
