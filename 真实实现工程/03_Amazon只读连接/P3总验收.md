# P3 Amazon只读连接｜阶段总验收

## 总结论

`PASS / P3_STAGE_MACHINE_VERIFIED`

P3 已完成离线真实运行实现与阶段级机器验证，覆盖 Amazon 只读 Connector、SP-API 只读能力、Ads API 只读能力、LWA access-token 刷新、限流/超时有界重试以及阶段联合门禁。

该结论**不等于**生产 Amazon 已接通。

## 完成项

1. `01_只读Connector` — `CONNECTOR_CONTRACT_VERIFIED`
2. `02_SPAPI只读能力` — `SPAPI_READ_CAPABILITY_VERIFIED`
3. `03_AdsAPI只读能力` — `ADSAPI_READ_CAPABILITY_VERIFIED`
4. `04_认证刷新与限流集成` — `AUTH_REFRESH_RATE_LIMIT_VERIFIED`
5. `05_P3阶段机器验收` — `P3_STAGE_MACHINE_VERIFIED`

## 最终机器证据

- 分支：`implementation/real-v1`
- P3 workflow：`real-v1-p3-ci`
- run id：`33305719435`
- job id：`99241819198`
- implementation head：`9f7c2d8fdf408fbc1c304bdda291b17ed5d4859d`
- PR merge test commit：`c7625b83c3b2155177e7fb3dce1c89fbe9e13fcf`
- 结果：`48 passed in 0.28s`
- conclusion：`success`
- P1 regression：run `33305719469` / `success`
- P2 regression：run `33305719437` / `success`

## 已具备能力

### Connector
- 固定 ReadOperation；
- API family 路由；
- SourceTrace；
- bounded retry；
- SecretRef credential boundary。

### SP-API
- NA / EU / FE 官方 endpoint allowlist；
- Catalog Items GET；
- Listings Items GET；
- FBA Inventory GET；
- 401/403 auth fail closed；
- 429 rate-limit；
- timeout；
- request id / rate-limit / HTTP status trace。

### Ads API
- NA / EU / FE 官方 endpoint allowlist；
- Sponsored Products campaigns list；
- ad groups list；
- keywords list；
- semantic-read POST allowlist；
- seller/marketplace → Ads profile 受控解析；
- OAuth access token / client id / scope headers；
- 401/403/429/timeout/sanitized HTTP error。

### Auth / Rate Limit
- LWA refresh-token 流程；
- access-token 内存缓存与过期刷新；
- dynamic SecretProvider；
- Retry-After；
- 指数退避；
- max attempts <= 5；
- max delay 有界；
- fake clock / fake sleeper / fake OAuth client 测试。

## 明确禁止 / 未实现

- Amazon 经营写能力；
- Ads create/update/delete/archive；
- 自动调价、自动竞价、自动预算修改；
- 自动审批；
- Executor；
- 未经授权生产动作。

## Secret事实

仓库未保存真实 Amazon credential。测试和阶段机器门禁没有调用生产 Amazon 或 LWA。Secret 值不会进入 SourceTrace、普通异常、普通日志、任务文件或 Git。

## 生产成熟度

- `AUTH_VERIFIED = PENDING`
- `READ_VERIFIED = PENDING`
- `LIVE_DATA_VERIFIED = PENDING`

要升级上述状态，后续必须通过独立的真实授权接入流程，以受保护 Secret Store 注入真实 seller / marketplace / Ads profile 授权，并执行有界只读验证。不能使用本阶段 Fake/Mock 机器结果替代生产验证。

## 下一阶段

按总工程顺序，下一个候选阶段为 `P4 MetricSnapshot事实层`。本次 P3 施工完成后不提前创建 P4 空目录；必须由任务总表在下一轮正式移动指针后再施工。
