# 系统运行层

本目录负责让各 Agent 真正被程序装载和运行，是整个智能体系统的“发动机舱”。

## 规划子模块
- 大模型运行时：负责调用大模型、管理模型配置与推理请求。
- Agent运行器：负责装载 Agent 的目标、提示词、Skill、规则和上下文，并驱动执行循环。
- 上下文管理：负责组织当前任务、产品、历史和跨 Agent 上下文。

## 原则
- Agent 负责岗位智能与决策逻辑。
- 系统运行层负责把 Agent 的静态定义变成可执行过程。
- 所有 Agent 共用统一运行时，避免重复建设。
